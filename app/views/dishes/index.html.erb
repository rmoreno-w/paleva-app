<h2>Pratos do Meu Restaurante</h2>

<% if @dishes.empty? %>
    <p>Ainda não há pratos cadastrados para o seu restaurante.</p>
<% else %>
    <ul>
        <% @dishes.each do |dish| %>
            <%= link_to restaurant_dish_path(@restaurant.id, dish.id) do %>
                <li><%= dish.name%></li>
            <% end %>
        <% end %>
    </ul>
<% end %>

<%= link_to 'Criar Prato', new_restaurant_dish_path(current_user.restaurant) %>