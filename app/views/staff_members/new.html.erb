<section class="flex flex-col gap-6">
    <h2>Pré-Cadastro</h2>
    <p>Aqui você faz um pré-registro de seu funcionário, informando alguns dados. Assim que ele se cadastrar na plataforma, nosso sistema reconhece automaticamente e o associa a seu Restaurante! :)</p>
</section>

<h3 class="text-projectPurple mt-10">Informe os dados do funcionário para o Pré-Cadastro</h3>

<%= form_with(url: restaurant_staff_members_path(@restaurant), method: :post, class: 'form mt-6') do |f| %>
    <div class="flex flex-col gap-2">
        <%= f.label "staff_member[#{:registration_number}]", PreRegistration.human_attribute_name(:registration_number), class: 'input_label' %>
        <%= f.text_field "staff_member[#{:registration_number}]", autocomplete: "registration_number", class: 'input' %>
    </div>

    <div class="flex flex-col gap-2">
        <%= f.label "staff_member[#{:email}]", PreRegistration.human_attribute_name(:email), class: 'input_label' %>
        <%= f.email_field "staff_member[#{:email}]", autocomplete: "email", class: 'input' %>
    </div>

    <%= f.submit 'Cadastrar', class: 'form_button' %>
<% end %>